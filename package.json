{
  "name": "wix-hubspot-integration",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "wix-app"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:wix": "npm run dev --workspace=wix-app",
    "build:backend": "npm run build --workspace=backend",
    "build:wix": "npm run build --workspace=wix-app",
    "db:migrate": "npm run db:migrate --workspace=backend"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "ws": "^8.21.0"
  }
}
